{#
{% from "partials/detailsWithIcons.njk" import detailsWithIcons %}
{{ detailsWithIcons(
  title="What Sets Us Apart",
  subtitle="nullEDGE is different from typical conferences. Here's what makes us unique.",
  items=[
    {
      icon: "fa-solid fa-hand-holding-dollar",
      title: "100% Free to Attend",
      description: "Community funded events with zero cost to attend. No credit card required, no hidden fees, just pure learning and networking."
    },
    {
      icon: "fa-solid fa-chalkboard-user",
      title: "Educational over Promotional",
      description: "Smart folks sharing real-world knowledge they've earned slaying proverbial dragons."
    },
    {
      icon: "fa-solid fa-calendar-days",
      title: "Global + Regional Events",
      description: "Events across multiple timezones, connecting you with the community in your region and worldwide."
    },
    {
      icon: "fa-solid fa-location-arrow",
      title: "Real World Focus",
      description: "Real implementations, actual problems solved, and lessons learned from the trenches—not theoretical concepts."
    }
  ],
)}}
#}

{% macro detailsWithIcons(title, subtitle="", items=[]) %}
<section class="differentiators-section">
  <div class="container">
    <h2 class="section-title">{{ title }}</h2>
    <p class="section-subtitle">{{ subtitle }}</p>
    
    <div class="differentiators-grid">
      {% for item in items %}
      <div class="card differentiator-card">
        <div class="card-icon">
          <span class="{{ item.icon }}" style="font-size:32px;"></span>
        </div>
        <h3>{{ item.title }}</h3>
        <p>{{ item.description }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

<style>
  /* Differentiators Section Styles */
  .differentiators-section {
    padding: 5rem 1rem;
    background: hsl(var(--muted) / 0.3);
  }

  .section-title {
    text-align: center;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    text-align: center;
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: hsl(var(--muted-foreground));
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  .differentiators-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 1400px;
    margin: 0 auto;
  }

  .differentiator-card {
    text-align: center;
  }

  .card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 4rem;
    height: 4rem;
    margin: 0 auto 1.5rem;
    background: hsl(var(--primary) / 0.1);
    border-radius: calc(var(--radius) - 2px);
    color: hsl(var(--primary));
  }

  .differentiator-card h3 {
    font-size: var(--size-step-1);
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .differentiator-card p {
    color: hsl(var(--muted-foreground));
    line-height: 1.6;
  }

  @media (max-width: 1024px) {
    .differentiators-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .differentiators-section {
      padding: 3rem 1rem;
    }

    .differentiators-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
{% endmacro %}