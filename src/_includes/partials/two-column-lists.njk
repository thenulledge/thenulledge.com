{#
Two Column Lists Partial - Side-by-side lists for comparisons or related info

Usage:
{% from "partials/two-column-lists.njk" import twoColumnLists %}
{{ twoColumnLists(
  title="Who Should Attend",
  leftTitle="Perfect For:",
  leftItems=[
    "ServiceNow developers and architects",
    "Administrators looking to connect with peers",
    "Consultants expanding their network"
  ],
  rightTitle="You'll Be Able To:",
  rightItems=[
    "Meet ServiceNow professionals from around the world",
    "Share challenges and solutions with peers",
    "Build your professional network"
  ]
) }}
#}

{% macro twoColumnLists(title="", leftTitle="", leftItems=[], rightTitle="", rightItems=[]) %}
<section class="two-column-lists-section">
  <div class="container">
    {% if title %}
    <h2 class="section-title">{{ title }}</h2>
    {% endif %}
    
    <div class="card two-column-lists-card">
      <div class="two-column-grid">
        <div class="list-column">
          {% if leftTitle %}
          <h3 class="list-title">{{ leftTitle }}</h3>
          {% endif %}
          <ul class="styled-list">
            {% for item in leftItems %}
            <li>{{ item }}</li>
            {% endfor %}
          </ul>
        </div>

        <div class="list-column">
          {% if rightTitle %}
          <h3 class="list-title">{{ rightTitle }}</h3>
          {% endif %}
          <ul class="styled-list">
            {% for item in rightItems %}
            <li>{{ item }}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Two Column Lists Section Styles */
  .two-column-lists-section {
    padding: 4rem 1rem;
  }

  .two-column-lists-section .section-title {
    text-align: center;
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 700;
    margin-bottom: 2rem;
  }

  .two-column-lists-card {
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
    border-radius: var(--radius);
    padding: var(--space-l);
    max-width: 1200px;
    margin: 0 auto;
  }

  .two-column-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem;
  }

  .list-column {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .list-title {
    font-size: var(--size-step-1);
    font-weight: 600;
    margin: 0 0 0.5rem 0;
    color: hsl(var(--foreground));
  }

  .styled-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .styled-list li {
    padding-left: 1.5rem;
    position: relative;
    line-height: 1.6;
    color: hsl(var(--muted-foreground));
  }

  .styled-list li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: hsl(var(--primary));
    font-weight: 700;
  }

  @media (max-width: 768px) {
    .two-column-lists-section {
      padding: 3rem 1rem;
    }

    .two-column-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .two-column-lists-card {
      padding: var(--space-m);
    }
  }
</style>
{% endmacro %}
