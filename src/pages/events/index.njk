---
layout: base
title: 'All Events - nullEDGE'
description: 'Complete archive of all nullEDGE events from 2025 to 2026'
permalink: /events/index.html
---

<section class="events-archive">
  <div class="container">
    <header class="archive-header">
      <h1>All Events</h1>
      <p class="archive-description">Browse nullEDGE events by year.</p>
    </header>
    
    <div class="years-grid">
      {# 2026 - Upcoming #}
      <a href="/2026/" class="year-card year-card-upcoming">
        <span class="year-badge">2026</span>
        <h2 class="year-title">2026 Events</h2>
        <p class="year-description">Four events throughout the year. Join us for technical sessions, networking, and community.</p>
        <div class="year-stats">
          <span class="year-stat">
            <i class="fa-solid fa-calendar"></i>
            4 Events
          </span>
          <span class="year-stat">
            <i class="fa-solid fa-arrow-right"></i>
            View 2026
          </span>
        </div>
      </a>
      
      {# 2025 - Past #}
      <a href="/2025/" class="year-card year-card-past">
        <span class="year-badge">2025</span>
        <h2 class="year-title">2025 Retrospective</h2>
        <p class="year-description">Our first year: 746 attendees, 30 sessions, and amazing community engagement.</p>
        <div class="year-stats">
          <span class="year-stat">
            <i class="fa-solid fa-users"></i>
            746 Attendees
          </span>
          <span class="year-stat">
            <i class="fa-solid fa-check-circle"></i>
            Completed
          </span>
        </div>
      </a>
    </div>
    
    <div class="archive-navigation">
      <a href="/" class="btn btn-primary">
        <i class="fa-solid fa-arrow-left"></i>
        Back to Home
      </a>
    </div>
  </div>
</section>

<style>
  .events-archive {
    padding: 4rem 1rem;
  }

  .container {
    max-width: 900px;
    margin: 0 auto;
  }

  .archive-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .archive-header h1 {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .archive-description {
    font-size: 1.25rem;
    color: hsl(var(--muted-foreground));
    max-width: 600px;
    margin: 0 auto;
  }

  /* Years Grid */
  .years-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .year-card {
    display: flex;
    flex-direction: column;
    padding: 2rem;
    background: hsl(var(--card));
    border: 2px solid hsl(var(--border));
    border-radius: var(--radius);
    text-decoration: none;
    color: inherit;
    transition: all 0.2s;
  }

  .year-card:hover {
    border-color: hsl(var(--primary));
    box-shadow: 0 8px 16px hsl(var(--primary) / 0.1);
  }

  .year-card-past {
    opacity: 0.8;
    background: hsl(var(--muted) / 0.3);
  }

  .year-card-past:hover {
    opacity: 0.9;
  }

  .year-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    padding: 0.375rem 0.75rem;
    border-radius: calc(var(--radius) - 4px);
    font-size: var(--size-step--2);
    font-weight: 700;
    margin-bottom: 1rem;
    width: fit-content;
  }

  .year-card-upcoming .year-badge {
    background: hsl(var(--primary) / 0.1);
    color: hsl(var(--primary));
  }

  .year-card-past .year-badge {
    background: hsl(var(--accent) / 0.5);
    color: hsl(var(--accent-foreground));
  }

  .year-title {
    font-size: var(--size-step-1);
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .year-description {
    color: hsl(var(--muted-foreground));
    line-height: 1.6;
    margin-bottom: 1.5rem;
    flex-grow: 1;
  }

  .year-stats {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .year-stat {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: var(--size-step--1);
    color: hsl(var(--muted-foreground));
  }

  .year-card-upcoming .year-stat i {
    color: hsl(var(--primary));
  }

  .year-card-past .year-stat i {
    color: hsl(var(--accent));
  }

  /* Navigation */
  .archive-navigation {
    text-align: center;
    margin-top: 3rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: var(--radius);
    font-weight: 600;
    text-decoration: none;
    transition: all 0.2s;
  }

  .btn-primary {
    background: hsl(var(--primary));
    color: hsl(var(--primary-foreground));
  }

  .btn-primary:hover {
    background: hsl(var(--primary) / 0.9);
  }

  @media (max-width: 768px) {
    .events-archive {
      padding: 2rem 1rem;
    }

    .years-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
