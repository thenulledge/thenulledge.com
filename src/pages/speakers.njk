---
layout: base
title: "nullEDGE Speakers"
description: "Meet the speakers who have presented at nullEDGE conferences. ServiceNow developers, architects, and community leaders sharing their expertise."
permalink: /speakers/
ogImage: /assets/og-images/page-speakers-preview.png
schema: CollectionPage
breadcrumb:
  parent: Home
  parentUrl: /
  current: Speakers
---

<div class="container wrapper flow">
  {# Hero Section #}
  <section class="prose text-center">
    <h1>nullEDGE Speakers</h1>
    <p class="lead">
      Meet the ServiceNow practitioners who share their expertise at nullEDGE conferences.
    </p>
  </section>

  {# Speakers Grid #}
  <section class="speakers-grid-section region">
    <div class="speakers-grid">
      {% set sortedSpeakers = collections.speakers | sort %}
      {% for speakerEntry in sortedSpeakers %}
        {% set name = speakerEntry[0] %}
        {% set speaker = speakerEntry[1] %}
        {% set speakerSlug = name | slugify %}
        <a href="/speakers/{{ speakerSlug }}/" class="speaker-card">
          <div class="speaker-card-image">
            <img 
              src="{{ speaker.image }}" 
              alt="{{ name }}"
              eleventy:ignore
            />
          </div>
          <div class="speaker-card-info">
            <h3 class="speaker-name">{{ name }}</h3>
            {% if speaker.title %}
              <p class="speaker-title">{{ speaker.title }}</p>
            {% endif %}
          </div>
        </a>
      {% endfor %}
    </div>
  </section>

  {# CTA Section #}
  <section class="region text-center">
    <div class="card flow prose cta-container">
      <h2>Want to Speak at nullEDGE?</h2>
      <p>Join these amazing speakers and share your ServiceNow knowledge with the community.</p>
      <div class="button-group">
        <a href="/submit-session/" class="btn btn-lg btn-primary">Submit Your Talk</a>
        <a href="/" class="btn btn-lg btn-outline">View Upcoming Events</a>
      </div>
    </div>
  </section>
</div>

<style>
  /* Speakers grid styles */
  .speakers-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(100%, 200px), 1fr));
    gap: var(--space-l);
  }

  .speaker-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
    transition: transform 0.2s ease;
  }

  .speaker-card:hover {
    transform: translateY(-4px);
  }

  .speaker-card-image {
    width: 100%;
    aspect-ratio: 1;
    border-radius: 50%;
    overflow: hidden;
    border: 3px solid hsl(var(--border));
    margin-bottom: var(--space-s);
    transition: border-color 0.2s ease;
  }

  .speaker-card:hover .speaker-card-image {
    border-color: hsl(var(--primary));
  }

  .speaker-card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .speaker-card-info {
    text-align: center;
  }

  .speaker-name {
    font-size: var(--size-step-0);
    font-weight: 600;
    margin: 0 0 0.25rem 0;
    color: hsl(var(--foreground));
  }

  .speaker-card:hover .speaker-name {
    color: hsl(var(--primary));
  }

  .speaker-title {
    font-size: var(--size-step--1);
    color: hsl(var(--muted-foreground));
    margin: 0;
    line-height: 1.3;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .cta-container {
    max-width: 800px;
    margin: 0 auto;
  }

  @media (max-width: 640px) {
    .speakers-grid {
      grid-template-columns: repeat(auto-fill, minmax(min(100%, 140px), 1fr));
      gap: var(--space-m);
    }

    .speaker-name {
      font-size: var(--size-step--1);
    }

    .speaker-title {
      font-size: var(--size-step--2);
    }
  }
</style>
