---
layout: base
title: "Submit a Talk | nullEDGE 2026"
description: "Submit your ServiceNow talk for nullEDGE 2026. 15-minute sessions, real code, no sales pitches."
permalink: /submit-session/
schema: CollectionPage
breadcrumb:
  parent: Home
  parentUrl: /
  current: Submit Session
---

<article class="submit-page">
  <header class="submit-hero">
    <h1>Submit a Talk</h1>
    <p>Share your ServiceNow knowledge. 15-minute sessions, real code, no sales pitches.</p>
  </header>

  {# Events accepting proposals #}
  <section class="submit-events">
    <h2>Events Accepting Proposals</h2>
    
    {% set events2026Sorted = events['2026'] | eventsToArray %}
    {% set hasOpenCFP = false %}
    
    <div class="events-list">
      {% for event in events2026Sorted %}
        {% if event.speakerRegistrationUrl %}
          {% set hasOpenCFP = true %}
          <div class="event-card">
            <div class="event-info">
              <h3>{{ event.name }}</h3>
              <p class="event-date">{{ event.when.start | formatDate("MMMM D, YYYY") }}</p>
              {% if event.speakerSubmissionEnd %}
                <p class="event-deadline">Deadline: {{ event.speakerSubmissionEnd | formatDate("MMMM D, YYYY") }}</p>
              {% endif %}
            </div>
            <a href="{{ event.speakerRegistrationUrl }}" class="btn btn-primary" target="_blank" rel="noopener noreferrer">
              Submit Talk
            </a>
          </div>
        {% endif %}
      {% endfor %}
      
      {% if not hasOpenCFP %}
        <p class="no-cfp">No events are currently accepting proposals. Check back soon.</p>
      {% endif %}
    </div>
  </section>

  {# Guidelines #}
  <section class="submit-guidelines">
    <h2>Guidelines</h2>
    <dl class="guidelines-list">
      <div class="guideline">
        <dt>Session Length</dt>
        <dd>15 minutes</dd>
      </div>
      <div class="guideline">
        <dt>Format</dt>
        <dd>Virtual, recorded, published to YouTube</dd>
      </div>
      <div class="guideline">
        <dt>We Want</dt>
        <dd>Real code, actual implementations, lessons learned</dd>
      </div>
      <div class="guideline">
        <dt>We Don't Want</dt>
        <dd>Sales pitches, marketing, theoretical overviews</dd>
      </div>
    </dl>
  </section>

  {# FAQ - Just the essentials #}
  <section class="submit-faq">
    <h2>FAQ</h2>
    <dl class="faq-list">
      <div class="faq-item">
        <dt>Do I need to be an expert?</dt>
        <dd>No. If you've solved a real problem, you have something to share.</dd>
      </div>
      <div class="faq-item">
        <dt>Can I submit multiple talks?</dt>
        <dd>Yes, submit as many as you like.</dd>
      </div>
    </dl>
  </section>
</article>

<style>
  .submit-page {
    max-width: 700px;
    margin: 0 auto;
    padding: 2rem 1rem;
  }

  .submit-hero {
    text-align: center;
    padding: 2rem 0 3rem;
    border-bottom: 1px solid hsl(var(--border));
  }

  .submit-hero h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    margin-bottom: 0.5rem;
  }

  .submit-hero p {
    color: hsl(var(--muted-foreground));
    font-size: 1.125rem;
  }

  /* Events */
  .submit-events {
    padding: 2rem 0;
    border-bottom: 1px solid hsl(var(--border));
  }

  .submit-events h2 {
    margin-bottom: 1.5rem;
  }

  .events-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .event-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
    border-radius: var(--radius);
  }

  .event-info h3 {
    font-size: 1.125rem;
    margin: 0;
  }

  .event-date {
    font-size: 0.875rem;
    color: hsl(var(--muted-foreground));
    margin: 0.25rem 0 0;
  }

  .event-deadline {
    font-size: 0.75rem;
    color: hsl(var(--primary));
    margin: 0.25rem 0 0;
  }

  .no-cfp {
    color: hsl(var(--muted-foreground));
    font-style: italic;
  }

  /* Guidelines */
  .submit-guidelines {
    padding: 2rem 0;
    border-bottom: 1px solid hsl(var(--border));
  }

  .submit-guidelines h2 {
    margin-bottom: 1rem;
  }

  .guidelines-list {
    display: grid;
    gap: 0.75rem;
    margin: 0;
  }

  .guideline {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 1rem;
    padding: 0.5rem 0;
    border-bottom: 1px solid hsl(var(--border) / 0.5);
  }

  .guideline:last-child {
    border-bottom: none;
  }

  .guideline dt {
    font-weight: 600;
    font-size: 0.875rem;
    color: hsl(var(--muted-foreground));
  }

  .guideline dd {
    margin: 0;
  }

  /* FAQ */
  .submit-faq {
    padding: 2rem 0;
  }

  .submit-faq h2 {
    margin-bottom: 1rem;
  }

  .faq-list {
    margin: 0;
  }

  .faq-item {
    padding: 1rem 0;
    border-bottom: 1px solid hsl(var(--border) / 0.5);
  }

  .faq-item:last-child {
    border-bottom: none;
  }

  .faq-item dt {
    font-weight: 600;
    margin-bottom: 0.25rem;
  }

  .faq-item dd {
    margin: 0;
    color: hsl(var(--muted-foreground));
  }

  @media (max-width: 500px) {
    .event-card {
      flex-direction: column;
      align-items: flex-start;
    }

    .guideline {
      grid-template-columns: 1fr;
      gap: 0.25rem;
    }
  }
</style>
