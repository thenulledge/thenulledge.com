---
layout: base
permalink: /index.html
title: 'nullEDGE - Free ServiceNow Conference'
description: 'Join 1,000+ ServiceNow practitioners for deep technical sessions, real code, and zero sales pitches. 100% free virtual conference.'
---

{# Hero Section #}
<section class="hero-section">
  <div class="hero-gradient"></div>
  <div class="container hero-content">
    <div class="hero-inner">
      <div class="logo-wrapper">
        <img src="/assets/images/nullEDGElogo.jpg" alt="nullEDGE Logo" class="hero-logo">
      </div>
      
      <div class="badge-wrapper">
        <span class="badge badge-lg badge-primary">4 Events in 2026</span>
      </div>

      <h1 class="hero-title">
        Join 1,000+ ServiceNow Practitioners <span class="text-primary">FREE</span>
      </h1>

      <p class="hero-subtitle">
        Deep technical sessions, real code, and zero sales pitches
      </p>

      {# Embedded Video #}
      <div class="video-container">
        <div class="video-wrapper">
          <lite-youtube videoid="dU6JY136vTg"></lite-youtube>
        </div>
        <p class="video-caption">Watch highlights from nullEDGE 2025</p>
      </div>

      {# Event Info Icons #}
      <div class="event-info-grid">
        <div class="event-info-item">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
          <span>4 Events Throughout 2026</span>
        </div>
        <div class="event-info-item">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/></svg>
          <span>Virtual Events</span>
        </div>
        <div class="event-info-item">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          <span>50+ Sessions</span>
        </div>
      </div>

      {# CTA Buttons #}
      <div class="cta-buttons">
        <a href="#events" class="btn btn-lg btn-primary">View 2026 Events</a>
        <a href="/sponsors" class="btn btn-lg btn-outline">Become a Sponsor</a>
      </div>
    </div>
  </div>
</section>

{# 2025 Retrospective Stats #}
<section class="stats-section">
  <div class="container">
    <h2 class="section-title">nullEDGE 2025 Retrospective</h2>
    <p class="section-subtitle">Relive the inaugural nullEDGE conference</p>
    
    <div class="stats-grid">
      <div class="stat-item">
        <div class="stat-number">{{ meta.conference2025.attendees }}</div>
        <div class="stat-label">Attendees</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">{{ meta.conference2025.speakers }}</div>
        <div class="stat-label">Speakers</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">{{ meta.conference2025.sessions }}</div>
        <div class="stat-label">Sessions</div>
      </div>
    </div>
    
    <div class="text-center" style="margin-top: 2rem;">
      <a href="/2025/conference" class="btn btn-lg btn-outline">Watch Recordings & View Schedule</a>
    </div>
  </div>
</section>

{# What Sets Us Apart #}
<section class="differentiators-section">
  <div class="container">
    <h2 class="section-title">What Sets Us Apart</h2>
    <p class="section-subtitle">nullEDGE is different from typical conferences. Here's what makes us unique.</p>
    
    <div class="differentiators-grid">
      <div class="card differentiator-card">
        <div class="card-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" x2="12" y1="2" y2="22"/><path d="M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg>
        </div>
        <h3>100% Free to Attend</h3>
        <p>Community-funded events with zero cost to attend. No credit card required, no hidden fees, just pure learning and networking.</p>
      </div>
      
      <div class="card differentiator-card">
        <div class="card-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="m4.9 4.9 14.2 14.2"/></svg>
        </div>
        <h3>Zero Sales Pitches</h3>
        <p>Real practitioners sharing production code and actual implementations—not vendor demos or marketing presentations.</p>
      </div>
      
      <div class="card differentiator-card">
        <div class="card-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 2a14.5 14.5 0 0 0 0 20 14.5 14.5 0 0 0 0-20"/><path d="M2 12h20"/></svg>
        </div>
        <h3>Global + Regional Events</h3>
        <p>Four events across multiple timezones in 2026, connecting you with the ServiceNow community in your region and worldwide.</p>
      </div>
      
      <div class="card differentiator-card">
        <div class="card-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"/></svg>
        </div>
        <h3>Real-World Focus</h3>
        <p>Production implementations, actual problems solved, and lessons learned from the trenches—not theoretical concepts.</p>
      </div>
    </div>
  </div>
</section>

{# 2026 Events Timeline #}
<section class="events-section" id="events">
  <div class="container">
    <h2 class="section-title">2026 Event Series</h2>
    <p class="section-subtitle">Four events throughout 2026, connecting ServiceNow practitioners across APAC, EMEA, India, and globally.</p>
    
    <div class="events-grid">
      {% for event in events2026.events %}
      <a href="{{ event.url }}" class="event-card {% if event.format == 'full-day' %}event-card-featured{% endif %}">
        <h3 class="event-name">{{ event.shortName }}</h3>
        <div class="event-meta">
          <div class="event-date">
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect width="18" height="18" x="3" y="4" rx="2" ry="2"/><line x1="16" x2="16" y1="2" y2="6"/><line x1="8" x2="8" y1="2" y2="6"/><line x1="3" x2="21" y1="10" y2="10"/></svg>
            <span>{{ event.date | formatDateWithTimezone(event.timezone) }}</span>
          </div>
          <div class="event-format">
            {% if event.format == 'networking' %}
              <span class="format-badge format-networking">Networking Event</span>
            {% endif %}
            {% if event.format == 'sessions' %}
              <span class="format-badge format-sessions">{{ event.sessionCount }} Sessions</span>
            {% endif %}
            {% if event.format == 'full-day' %}
              <span class="format-badge format-conference">Full Day Conference</span>
            {% endif %}
          </div>
        </div>
        <p class="event-description">{{ event.description }}</p>
      </a>
      {% endfor %}
    </div>
  </div>
</section>

{# Sponsors #}
<section class="sponsors-section">
  <div class="container">
    <h2 class="section-title">Our 2025 Sponsors</h2>
    <p class="section-subtitle">Thank you to these organizations for supporting community-driven learning</p>
    
    <div class="sponsors-grid">
      {% for sponsor in sponsors.slice(0, 8) %}
      <div class="sponsor-item">
        <a href="{{ sponsor.website }}" target="_blank" rel="noopener noreferrer">
          <img src="/assets/images{{ sponsor.logo }}" alt="{{ sponsor.name }}" loading="lazy">
        </a>
      </div>
      {% endfor %}
    </div>
    
    <div class="text-center" style="margin-top: 3rem;">
      <a href="/sponsors" class="btn btn-lg btn-primary">Become a 2026 Sponsor</a>
    </div>
  </div>
</section>

<style>
  /* Hero Section - Custom Styles */
  .hero-section {
    position: relative;
    padding: 5rem 1rem;
    overflow: hidden;
  }

  .hero-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, 
      color-mix(in srgb, var(--color-primary) 10%, transparent),
      var(--color-bg),
      color-mix(in srgb, var(--color-secondary) 10%, transparent)
    );
  }

  .hero-content {
    position: relative;
    z-index: 10;
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-inner {
    text-align: center;
  }

  .logo-wrapper {
    margin-bottom: 2rem;
  }

  .hero-logo {
    max-width: 600px;
    width: 100%;
    height: auto;
  }

  .badge-wrapper {
    margin-bottom: 1.5rem;
  }

  .hero-title {
    font-size: clamp(2rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 1rem;
  }

  .text-primary {
    color: hsl(var(--primary));
  }

  .hero-subtitle {
    font-size: clamp(1.125rem, 2vw, 1.5rem);
    color: hsl(var(--muted-foreground));
    margin-bottom: 2rem;
  }

  /* Video Styles */
  .video-container {
    max-width: 800px;
    margin: 2rem auto;
  }

  .video-wrapper {
    position: relative;
    padding-bottom: 56.25%;
    height: 0;
    overflow: hidden;
    border-radius: calc(var(--radius) - 2px);
  }

  .video-wrapper lite-youtube {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
  }

  .video-caption {
    margin-top: 0.5rem;
    font-size: var(--size-step--1);
    color: hsl(var(--muted-foreground));
    text-align: center;
  }

  /* Event Info Icons */
  .event-info-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem;
    margin: 2rem 0;
  }

  .event-info-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: hsl(var(--muted-foreground));
  }

  .event-info-item svg {
    color: hsl(var(--primary));
  }

  .cta-buttons {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    margin-top: 2rem;
  }

  /* Sections */
  .stats-section,
  .differentiators-section,
  .events-section,
  .sponsors-section {
    padding: 5rem 1rem;
  }

  .differentiators-section {
    background: hsl(var(--muted) / 0.3);
  }

  .section-title {
    text-align: center;
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    text-align: center;
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: hsl(var(--muted-foreground));
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  /* Stats Grid */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin: 2rem 0;
    text-align: center;
  }

  .stat-number {
    font-size: 3rem;
    font-weight: 700;
    color: hsl(var(--primary));
  }

  .stat-label {
    font-size: var(--size-step-0);
    color: hsl(var(--muted-foreground));
  }

  /* Differentiators Grid */
  .differentiators-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(280px, 100%), 1fr));
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .differentiator-card {
    text-align: center;
  }

  .card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 4rem;
    height: 4rem;
    margin: 0 auto 1.5rem;
    background: hsl(var(--primary) / 0.1);
    border-radius: calc(var(--radius) - 2px);
    color: hsl(var(--primary));
  }

  .differentiator-card h3 {
    font-size: var(--size-step-1);
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .differentiator-card p {
    color: hsl(var(--muted-foreground));
    line-height: 1.6;
  }

  /* Events Grid */
  .events-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(280px, 100%), 1fr));
    gap: 2rem;
  }

  .event-card {
    position: relative;
    padding: 2rem;
    background: hsl(var(--card));
    border: 2px solid hsl(var(--border));
    border-radius: var(--radius);
    text-decoration: none;
    color: inherit;
    transition: all 0.2s;
  }

  .event-card:hover {
    border-color: hsl(var(--primary));
    box-shadow: 0 8px 16px hsl(var(--primary) / 0.1);
  }

  .event-card-featured {
    grid-column: 1 / -1;
  }

  .event-name {
    font-size: var(--size-step-1);
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .event-meta {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 1rem;
    font-size: var(--size-step--1);
  }

  .event-date {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .event-format {
    display: flex;
    align-items: center;
  }

  .format-badge {
    display: inline-flex;
    padding: 0.25rem 0.75rem;
    border-radius: calc(var(--radius) - 2px);
    font-size: var(--size-step--2);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.025em;
  }

  .format-networking {
    background: hsl(var(--primary) / 0.1);
    color: hsl(var(--primary));
  }

  .format-sessions {
    background: hsl(var(--accent) / 0.5);
    color: hsl(var(--accent-foreground));
  }

  .format-conference {
    background: hsl(var(--primary));
    color: hsl(var(--primary-foreground));
  }

  .event-description {
    color: hsl(var(--muted-foreground));
    line-height: 1.6;
  }

  /* Sponsors Grid */
  .sponsors-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    align-items: center;
  }

  .sponsor-item {
    text-align: center;
  }

  .sponsor-item img {
    max-height: 80px;
    width: auto;
    max-width: 100%;
  }

  .text-center {
    text-align: center;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .hero-section {
      padding: 3rem 1rem;
    }

    .stats-section,
    .differentiators-section,
    .events-section,
    .sponsors-section {
      padding: 3rem 1rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      text-align: center;
    }
  }

  /* Lite YouTube Embed Styles */
  {% include "../assets/css/local/lite-youtube.css" %}
</style>

<script type="module">
  import 'https://cdn.jsdelivr.net/npm/@justinribeiro/lite-youtube@1.5.0/lite-youtube.js';
</script>
